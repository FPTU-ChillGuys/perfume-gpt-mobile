import 'package:flutter/material.dart';

class ImportTicketListScreen extends StatelessWidget {
  const ImportTicketListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Danh sách đơn nhập hàng'),
      ),
      body: const Center(
        child: Text('Import Ticket List Screen - Placeholder'),
      ),
    );
  }
}
