class ProductScentNote {
  final int? noteId;
  final String name;
  final String type; // Top, Heart, Base

  const ProductScentNote({
    this.noteId,
    required this.name,
    required this.type,
  });
}
